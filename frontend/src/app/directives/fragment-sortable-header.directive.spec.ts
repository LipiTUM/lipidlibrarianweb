import { FragmentSortableHeaderDirective } from './fragment-sortable-header.directive';


describe('FragmentSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
