import { AdductSortableHeaderDirective } from './adduct-sortable-header.directive';


describe('AdductSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AdductSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
