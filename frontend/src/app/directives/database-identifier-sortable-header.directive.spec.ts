import { DatabaseIdentifierSortableHeaderDirective } from './database-identifier-sortable-header.directive';


describe('DatabaseIdentifierSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DatabaseIdentifierSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
