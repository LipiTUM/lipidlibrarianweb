import { SynonymSortableHeaderDirective } from './synonym-sortable-header.directive';


describe('SynonymSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SynonymSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
