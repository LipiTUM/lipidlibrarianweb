import { AtLeastOneBoxCheckedDirective } from './at-least-one-box-checked.directive';


describe('AtLeastOneBoxCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new AtLeastOneBoxCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
