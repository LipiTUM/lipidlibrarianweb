import { StructureIdentifierSortableHeaderDirective } from './structure-identifier-sortable-header.directive';


describe('StructureIdentifierSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new StructureIdentifierSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
