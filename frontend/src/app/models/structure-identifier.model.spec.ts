import { StructureIdentifier } from './structure-identifier.model';


describe('StructureIdentifier', () => {
  it('should create an instance', () => {
    expect(new StructureIdentifier()).toBeTruthy();
  });
});
