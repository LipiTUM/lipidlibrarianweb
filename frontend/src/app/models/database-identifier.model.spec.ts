import { DatabaseIdentifier } from './database-identifier.model';


describe('DatabaseIdentifier', () => {
  it('should create an instance', () => {
    expect(new DatabaseIdentifier()).toBeTruthy();
  });
});
