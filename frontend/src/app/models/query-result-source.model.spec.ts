import { QueryResultSource } from './query-result-source.model';


describe('QueryResultSource', () => {
  it('should create an instance', () => {
    expect(new QueryResultSource()).toBeTruthy();
  });
});
