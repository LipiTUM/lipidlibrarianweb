import { OntologyGraphNode } from './ontology-graph-node.model';


describe('OntologyGraphNode', () => {
  it('should create an instance', () => {
    expect(new OntologyGraphNode()).toBeTruthy();
  });
});
