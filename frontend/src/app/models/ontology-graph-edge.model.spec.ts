import { OntologyGraphEdge } from './ontology-graph-edge.model';


describe('OntologyGraphEdge', () => {
  it('should create an instance', () => {
    expect(new OntologyGraphEdge()).toBeTruthy();
  });
});
